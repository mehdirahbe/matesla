static files will be placed here by collect static. Directory must exists or collectstatic will fail.

collectstatic must be run manually when new static files are added for local test. It is run automatically for deployement in git

Which means that this directory must be ignored in git
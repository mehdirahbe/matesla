# Python 3.12+ — stack modernisé 2026 (projet ressuscité hors Heroku 2021)
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -U pip && pip install -r requirements.txt

# --- Web framework ---
Django>=5.2.16,<5.3          # LTS jusqu'à avril 2028
gunicorn>=23.0.0
whitenoise>=6.9.0

# --- Database ---
dj-database-url>=2.3.0
# Optionnel : Postgres si DATABASE_URL est défini. En local = SQLite par défaut.
psycopg[binary]>=3.2.0

# --- HTTP / OAuth (Fleet API) ---
requests>=2.32.0
requests-oauthlib>=2.0.0
python-dotenv>=1.1.0

# --- Features app ---
geopy>=2.4.1
Pillow>=11.0.0
matplotlib>=3.9.0
numpy>=2.0.0
django-tables2>=2.7.0

# --- Dev ---
django-debug-toolbar>=5.0.0

# --- Email (reset password, optionnel sans clé) ---
django-sendgrid-v5>=1.2.4
